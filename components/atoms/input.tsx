import React from "react";

const Input = () => {
  return (
    <input
      type="text"
      className="bg-transparent  focus:outline-none focus-visible:outline-none placeholder:text-sm"
      placeholder="Search something..."
    />
  );
};

export default Input;
