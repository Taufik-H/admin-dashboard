export interface SidebarMenuProps {
  name: string;
  path: string;
  icon: JSX.Element | string;
}

export interface ActivityProps {
  image: string;
  name: string;
  activity: string;
  time: string;
}
